/* global Item */

/**
 * Extend the base Item entity for DCC RPG
 * @extends {Item}
 */
class DCCItem extends Item {
  prepareData () {
    super.prepareData()
  }
}

export default DCCItem
